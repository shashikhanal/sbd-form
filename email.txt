// //Send response.csv to email
// $email = new PHPMailer();
// $email->From      = 'form@server.com';
// $email->FromName  = 'Large Outlet SBD Visibility';
// $email->Subject   = 'Someone filled up the form';
// $email->Body      = "Latest copy of response.csv";
// $email->AddAddress( 'shashi@evolve.com.np' );
// // $email->AddAddress( 'arbinde@evolveasia.co' );
//
// $file_to_attach = 'response.csv';
//
// $email->AddAttachment( $file_to_attach , 'response.csv' );
// $result = $email->Send();

$url = 'https://api.sendgrid.com/';
$user = 'shashikhanal';
$pass = 'qwerty123';

$fileName = 'response.csv';
$filePath = dirname(__FILE__);

$params = array(
    'api_user'  => $user,
    'api_key'   => $pass,
    'to'        => 'khanalshashi@gmail.com',
    'subject'   => 'Someone filled up the form',
    'html'      => 'Latest copy of response.csv',
    'text'      => 'text message',
    'from'      => 'form@server.com',
    'files['.$fileName.']' => $fileName
  );

// print_r($params);

$request =  $url.'api/mail.send.json';

// Generate curl request
$session = curl_init($request);

// Tell curl to use HTTP POST
curl_setopt ($session, CURLOPT_POST, true);

// Tell curl that this is the body of the POST
curl_setopt ($session, CURLOPT_POSTFIELDS, $params);

// Tell curl not to return headers, but do return the response
curl_setopt($session, CURLOPT_HEADER, false);
// Tell PHP not to use SSLv3 (instead opting for TLS)
curl_setopt($session, CURLOPT_SSLVERSION, CURL_SSLVERSION_TLSv1_2);
curl_setopt($session, CURLOPT_RETURNTRANSFER, true);

// obtain response
$response = curl_exec($session);
curl_close($session);

// print everything out
// print_r($response);
